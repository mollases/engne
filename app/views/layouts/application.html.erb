<!DOCTYPE html>
<html dir="ltr" lang="en-US">
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
		<title>ENGNE</title>
	  <%= stylesheet_link_tag    "application", :media => "all" %>
	  <%= javascript_include_tag "application" %>
	  <%= csrf_meta_tags %>
	</head>
	<body>
		<div id="page">
			<div id="header" class="padd">
				<div class="logo centerized"><%= link_to "EngnE", index_path %></div>
			</div>
			<div id="profile" class="padd">
				<% if member_signed_in? %>
					Currently in as <%= link_to current_member.username, member_path(current_member) %>.
					<%= link_to "Log out", destroy_member_session_path, :method=>'delete' %>?
				<% else %>		
					<%= link_to (new_member_registration_path) do %><span class="bigBlue">Sign up</span> <% end %> or
					<%= link_to (new_member_session_path) do %><span class="bigBlue">Sign in</span> <% end %>
				<% end %>
			</div>
			<div id="main" class="padd cf">
				<% flash.each do |name, msg| %>
					<%= content_tag :div, msg, :class => "flash", :id => "#{name}"%>
				<% end %>
				<%= yield %>
			</div>
			<div id="footer" class="padd">
				<div id="left-footer-bar" class="footer-bar padd"> stuffing </div>
				<div id="center-right-footer-bar">
					<div id="center-footer-bar" class="footer-bar padd"> stuffing </div>
					<div id="right-footer-bar"  class="footer-bar padd"> stuffing </div>
				</div>
			</div>
		</div>
	</body>
</html>